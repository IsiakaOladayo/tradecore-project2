# Trivy exceptions. AVD- and legacy IDs both match; legacy listed for readability.
# AWS-0053: internet-facing ALB by design (private ALB needs NAT/VPC endpoints, out of scope).
AVD-AWS-0053
# AWS-0054: port-80 listener is redirect-to-HTTPS only (plus HSTS); Trivy flags
# the resource regardless of its redirect action. TLS itself is live.
AVD-AWS-0054
# AWS-0104: ECS 443-only egress (no VPC endpoints, no NAT by budget scope).
AVD-AWS-0104
# AWS-0164: public IPs by design, no NAT per $30 budget.
AVD-AWS-0164
# AWS-0132: S3 AES256 by decision (CMK out of budget scope).
AVD-AWS-0132
# AWS-0136: SNS AWS-managed key by decision (CMK out of budget scope).
AVD-AWS-0136
